# 📋 Просмотр логов бота

## Расположение логов

Логи бота сохраняются в файл: **`logs/bot.log`**

## Способы просмотра логов

### 1. Через PowerShell скрипт (рекомендуется)

```powershell
# Последние 50 строк
.\view_logs.ps1

# Последние 100 строк
.\view_logs.ps1 -Lines 100

# Отслеживание в реальном времени (как tail -f)
.\view_logs.ps1 -Follow

# Только ошибки
.\view_logs.ps1 -Errors
```

### 2. Через PowerShell напрямую

```powershell
# Последние 50 строк
Get-Content logs\bot.log -Tail 50

# Последние 100 строк
Get-Content logs\bot.log -Tail 100

# Отслеживание в реальном времени
Get-Content logs\bot.log -Wait -Tail 50

# Только ошибки
Get-Content logs\bot.log | Select-String "ERROR"
```

### 3. Через любой текстовый редактор

Откройте файл `logs/bot.log` в любом текстовом редакторе (Notepad++, VS Code, и т.д.)

## Формат логов

```
2026-01-20 22:56:42 - bot.main - INFO - Бот инициализирован
2026-01-20 22:56:42 - __main__ - INFO - Бот запущен!
2026-01-20 22:57:15 - bot.utils.listing_service - INFO - Получено 1 объявлений с Onliner
2026-01-20 22:57:16 - bot.utils.listing_service - ERROR - Ошибка парсинга Kufar: ...
```

## Уровни логирования

- **INFO** - Информационные сообщения (нормальная работа)
- **WARNING** - Предупреждения
- **ERROR** - Ошибки
- **DEBUG** - Отладочная информация (не используется в продакшене)

## Ротация логов

- Максимальный размер файла: **10 MB**
- Количество резервных файлов: **5**
- Старые логи сохраняются как: `bot.log.1`, `bot.log.2`, и т.д.

## Полезные команды

```powershell
# Размер файла логов
(Get-Item logs\bot.log).Length / 1MB

# Количество строк в логе
(Get-Content logs\bot.log | Measure-Object -Line).Lines

# Поиск по логам
Get-Content logs\bot.log | Select-String "пользователь"

# Очистить логи (осторожно!)
Remove-Item logs\bot.log
```
